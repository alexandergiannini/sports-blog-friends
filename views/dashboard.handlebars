  
    <div class="mb-5 pt-3">
        <h2 class="title pb-4 has-text-centered">Dashboard</h2>
        
        <a href="/dashboard/create/"><button id="create-post" class="button is-primary">Create New Post</button></a>
        
        
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 btn-comment">Edit Post</button></a>
        </div>
    {{/each}}

{{else}}
<h4 class="pb-4 user-content dash-post">You don't have any posts</h4>

{{/if}}