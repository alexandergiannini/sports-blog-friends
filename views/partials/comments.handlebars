  <div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="box">
    <div class="meta">
      <strong>
      Comment by {{user.username}} on {{format_date created_at}}
      </strong>
    </div>
    <div class="text">
      {{comment_text}}
    </div>
    </div>
  </section>
  {{/each}}
</div>